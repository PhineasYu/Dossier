# Add Today 快捷入口

## 改动
- 在所有页面的底部导航栏上方统一增加三个快捷按钮：语音、照片、文字。
- 语音和文字按钮直接进入 Capture 页面，并自动打开对应输入方式。
- 照片按钮直接进入 Archive 页面，并自动唤起照片选择器。
- 保留现有底部导航结构，确保快捷栏在手机宽度下不遮挡内容。

## 技术细节
- 把快捷入口集中放进共享底部导航组件，避免每个页面重复实现。
- 通过页面参数触发 Capture 的语音/文字模式与 Archive 的文件选择。
- 补齐所有内容页面的社交分享元信息，并检查桌面与手机显示。
